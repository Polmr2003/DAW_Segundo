Exemple 1: DOCUMENTACI� D'UNA FUNCI�
/**
   * Determina el volumen de un cilindro con la altura y radio especificado
   * @param {number} radio -  El radio de los c�rculos laterales del cilindro
   * @param {number} altura - La altura del cilindro
   * @return {number}
*/
 this.volumen = function obtenerVolumen(radio, altura) {
    const volumen = (Math.PI * Math.pow(radio, 2) * altura);
    return volumen;
  }


Exemple 2: DOCUMENTACI� D'UN FITXER
/**
   * @file Manages the configuration settings for the widget.
   * @author Pep Garcia
*/


Exemple 3: DOCUMENTACI� DE CODI
/*
|--------------------------------------------------------------------------
| Application Name
|--------------------------------------------------------------------------
|
| This value is the name of your application. This value is used when the
| framework needs to place the application's name in a notification or
| any other location as required by the application or its packages.
|
*/